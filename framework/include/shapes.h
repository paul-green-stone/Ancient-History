/* Used for basic shape utilities like drawing, collision detection, etc... */

#include <SDL2/SDL.h>
#include "./Math/Vector2D.h"

/**
 * Used to draw a circle
 */
void draw_circle(SDL_Renderer *renderer, Vector2D center, int radius, SDL_Color color);
